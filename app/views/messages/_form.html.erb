<%= simple_form_for ActsAsMessageable::Message.new, url: url, :method => :post do |f| %>
  <%= f.input :topic %>
  <%= f.input :body %>
  <%= f.button :submit, label: "Send", class: 'btn' %>
<% end %>

