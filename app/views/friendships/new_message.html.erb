<%= simple_form_for ActsAsMessageable::Message.new, :url => create_message_friends_path(@friend), :method => :post do |f| %>

 	<%= f.hidden_field :to, value: @to %>
  <%= f.input :topic %>
  <%= f.input :body %>
  <%= f.button :submit, label: "Send", class: 'btn' %>
<% end %>