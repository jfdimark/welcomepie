<h3> See who you can connect with today! </h3>
<% if current_user %>

		<% @users.each do |user| %>

			<% unless current_user == user %>
				<% unless current_user.friend_with? user = user %>

					<p><%= image_tag user.avatar.url(:thumb) %> 
						<br /><%=link_to user.name, user %>
			
				<% end %>

				<% unless current_user.friend_with? user = user %>

				<%= link_to "Add as friend", friends_path(:user_id => user), :method => :post, :class => "btn-small btn-success" %>

				<% end %>
			<% end %>
		<% end %></p>

		<% else %>

		<% @users.each do |user| %>

			<p>User: <%=link_to user.name, user %>

			<%= link_to "Sign up to make friends!", new_user_registration_path, :class => "btn-small btn-success" %> </p>
		<% end %>

	<% end %>