<%= simple_form_for ActsAsMessageable::Message.new, :url => create_message_users_path, :method => :post do |f| %>

 	<%= f.hidden_field :to, value: user %>
  <%= f.input :body %>
  <%= f.input :topic %>
  <%= f.button :submit, class: 'btn' %>
<% end %>